[project]
name = "log-triage"
version = "1.1.0"
description = "A command-line parser and triage tool for Linux authentication logs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0,<10",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
